name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  django:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: backend/requirements.txt
      - run: pip install -r requirements.txt
      - name: Check migrations
        env:
          DJANGO_SECRET_KEY: ci-only-secret
          DJANGO_DEBUG: "false"
        run: python manage.py makemigrations --check --dry-run --settings=config.test_settings
      - name: Run tests
        env:
          DJANGO_SECRET_KEY: ci-only-secret
          DJANGO_DEBUG: "false"
        run: python manage.py test --settings=config.test_settings

  docker:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - name: Validate Compose
        run: cp .env.example .env && docker compose config --quiet
      - name: Build web image
        run: docker build --tag rettungswache-wachbuch:test .
      - name: Reject runtime data in image
        run: |
          test -z "$(docker run --rm --entrypoint sh rettungswache-wachbuch:test -c \
            'find /app -type f \( -name "*.dump" -o -name "*.sqlite3" -o -name "*.pyc" \) -print')"
      - name: Start isolated PostgreSQL stack
        run: |
          sudo chown 70:70 backups
          HTTP_PORT=18091 docker compose up --build --detach web
      - name: Verify health and database restrictions
        run: |
          for attempt in $(seq 1 30); do
            curl --fail --silent http://127.0.0.1:18091/healthz/ && break
            test "$attempt" -lt 30
            sleep 2
          done
          test "$(docker compose exec -T db psql -U rwsth_owner -d rwsth -tAc \
            "SELECT has_table_privilege('rwsth_app', 'core_auditevent', 'UPDATE')")" = "f"
          test "$(docker compose exec -T db psql -U rwsth_owner -d rwsth -tAc \
            "SELECT has_column_privilege('rwsth_app', 'core_handoverentry', 'author_id', 'UPDATE')")" = "f"
          test "$(docker compose exec -T db psql -U rwsth_owner -d rwsth -tAc \
            "SELECT has_column_privilege('rwsth_app', 'core_handoverentry', 'status', 'UPDATE')")" = "t"
      - name: Show logs after failure
        if: failure()
        run: docker compose logs --no-color
      - name: Remove isolated stack
        if: always()
        run: docker compose down --volumes --remove-orphans

  trivy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - name: Trivy filesystem scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: fs
          scan-ref: backend
          severity: CRITICAL,HIGH
          exit-code: "1"
          ignore-unfixed: true
      - name: Build image for scan
        run: docker build --tag rettungswache-wachbuch:trivy ./backend
      - name: Trivy image scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: image
          image-ref: rettungswache-wachbuch:trivy
          severity: CRITICAL,HIGH
          exit-code: "1"
          ignore-unfixed: true

  client:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - name: Validate client assets
        run: |
          test -f client/index.html
          test -f client/manifest.webmanifest
          test -f client/sw.js
          test -f client/app.js
          ! grep -q "TODO_REMOVE" client/app.js
